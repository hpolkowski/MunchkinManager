<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <style name="menu_button">
        <item name="android:width">@dimen/menu_button_width</item>
        <item name="android:background">@color/colorAccent</item>
    </style>

    <style name="dice">
        <item name="android:minHeight">130dp</item>
        <item name="android:minWidth">130dp</item>
    </style>

    <style name="user_image_large">
        <item name="android:minHeight">@dimen/user_image_size_large</item>
        <item name="android:minWidth">@dimen/user_image_size_large</item>
    </style>

    <style name="user_image_medium">
        <item name="android:minHeight">@dimen/user_image_size_medium</item>
        <item name="android:minWidth">@dimen/user_image_size_medium</item>
    </style>

    <style name="button_arrow">
        <item name="android:layout_height">24dp</item>
        <item name="android:alpha">0.5</item>
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
    </style>

</resources>
